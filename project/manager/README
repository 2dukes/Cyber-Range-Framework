## Commands

- Create commands' FIFO: `mkfifo mypipe`
- Create cancel scenario FIFO: `mkfifo cancel_mypipe`
- Install websocketd: `sudo apt install websocketd`
- Run infinite loop of bash script echoing output via Web Socket: `./runWS.sh`
- Run cancel scenario bash script: `./runCancel.sh`

Start website with `docker-compose up`